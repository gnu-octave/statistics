%!demo
%! ## Compute the standard deviation for a Lognormal distribution
%! data = lognrnd (0, 1, 10000, 1);
%! pd = fitdist (data, "Lognormal");
%! std_value = std (pd)
%!
%! ## Use this to calculate the standard deviation, which measures the variability
%! ## in the positive skewed values of the distribution.
