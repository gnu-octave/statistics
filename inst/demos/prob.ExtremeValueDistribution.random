%!demo
%! ## Generate random samples from an Extreme Value distribution
%! rng (42);
%! randg ('state', 42);
%! rande ('state', 42);
%! randp ('state', 42);
%! pd = makedist ("ExtremeValue", "mu", 0, "sigma", 1)
%! samples = random (pd, 500, 1);
%! hist (samples, 50)
%! title ("Histogram of 500 random samples from ExtremeValue(mu=0, sigma=1)")
%! xlabel ("Values")
%! ylabel ("Frequency")
%!
%! ## This generates random samples from an Extreme Value distribution, useful
%! ## for simulating extreme events like maximum values in data sets.
