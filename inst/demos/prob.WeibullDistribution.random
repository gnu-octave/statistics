%!demo
%! ## Generate random samples from a Weibull distribution
%! rand ("seed", 21);
%! samples = wblrnd (1, 1, 500, 1);
%! hist (samples, 50)
%! title ("Histogram of 500 random samples from Weibull(lambda=1, k=1)")
%! xlabel ("values in x (x >= 0)")
%! ylabel ("Frequency")
%!
%! ## This generates random samples from a Weibull distribution, useful for
%! ## simulating lifetime data or positive skewed distributions.
