%!demo
%! ## Compute the median for different Beta distributions
%! pd1 = makedist ("Beta", "a", 1, "b", 1);
%! pd2 = makedist ("Beta", "a", 2, "b", 5);
%! median1 = median (pd1)
%! median2 = median (pd2)
%!
%! ## Use this to find the median value, which splits the beta distribution
%! ## into two equal probability halves.
