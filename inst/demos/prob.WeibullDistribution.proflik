%!demo
%! ## Compute and plot the profile likelihood for the shape parameter of a fitted
%! ## Weibull distribution
%! rand ("seed", 21);
%! data = wblrnd (1, 1, 1000, 1);
%! pd_fitted = fitdist (data, "Weibull");
%! [nlogL, param] = proflik (pd_fitted, 2, "Display", "on");
%!
%! ## Use this to analyze the profile likelihood of the shape parameter (k),
%! ## helping to understand the uncertainty in parameter estimates for failure rate models.
