%!demo
%! ## Compute and plot the profile likelihood for the mean parameter of a fitted
%! ## Exponential distribution
%! pd = makedist ("Exponential", "mu", 2)
%! rand ("seed", 5);
%! data = random (pd, 1000, 1);
%! pd_fitted = fitdist (data, "Exponential")
%! [nlogL, param] = proflik (pd_fitted, 1, "Display", "on");
%!
%! ## Use this to analyze the profile likelihood of the mean parameter (mu),
%! ## helping to understand the uncertainty in parameter estimates given the data.
